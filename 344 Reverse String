void reverseString(char* s, int sSize)
{
    int i = 0;
    int tmp;
    while (sSize > i)
    {
        tmp = s[sSize - 1];
        s[sSize - 1] = s[i];
        s[i] = tmp;
        sSize--;
        i++;
    }
}
